
#include <SDL2/SDL.h>
#include <SDL2/SDL_image.h>

#if defined(__APPLE__) && defined(__MACH__)
#include <OpenGL/gl.h>
#else
#include <GL/gl.h>
#endif

#ifndef CC_VIEWPORT_H
#define CC_VIEWPORT_H

class Viewport {
public:
  
};

#endif

